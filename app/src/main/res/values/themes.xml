<resources>
    <!-- Base application theme -->
    <style name="Base.Theme.Iara" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/background_init_gradient</item>
        <item name="android:statusBarColor">@color/dark_green</item>
    </style>

    <style name="Theme.SplashTheme" parent="Base.Theme.Iara">
        <item name="android:windowBackground">@drawable/background_init_gradient</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.Iara" parent="Base.Theme.Iara" />
</resources>
